// src/ai.ts
export type Color = "white" | "black";

// If you already have these types elsewhere, import instead of redefining.
export type GameState = any; // replace with your real type

export function pickAiMove(gs: GameState): GameState {
  // paste your existing pickAiMove implementation here
  // it must not reference React state, hooks, window, document, etc.
  return gs;
}
